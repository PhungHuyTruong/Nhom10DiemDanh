@model Nhom10ModuleDiemDanh.Models.PhuTrachXuongDto

@{
    Layout = null;
    var coSoList = ViewBag.CoSoList as List<SelectListItem>;
    var vaiTroList = ViewBag.VaiTroList as List<SelectListItem>;
}

<div class="row">
    <div class="col-md-4">
        <form id="editForm" asp-action="Edit" method="post">
            <input type="hidden" asp-for="IdNhanVien" />

            <div class="form-group">
                <label asp-for="TenNhanVien"></label>
                <input asp-for="TenNhanVien" class="form-control" />
                <span asp-validation-for="TenNhanVien" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="MaNhanVien"></label>
                <input asp-for="MaNhanVien" class="form-control" />
                <span asp-validation-for="MaNhanVien" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="EmailFE"></label>
                <input asp-for="EmailFE" class="form-control" />
                <span asp-validation-for="EmailFE" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="EmailFPT"></label>
                <input asp-for="EmailFPT" class="form-control" />
                <span asp-validation-for="EmailFPT" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label class="form-label">Cơ sở</label>
                @Html.DropDownListFor(m => m.IdCoSo, (IEnumerable<SelectListItem>)ViewBag.CoSoList, "-- Chọn cơ sở --", new { @class = "form-select" })
            </div>

            <div class="mb-3">
                <label class="form-label">Vai trò</label>
                @Html.ListBoxFor(m => m.IdVaiTros, (IEnumerable<SelectListItem>)ViewBag.VaiTroList, new { @class = "form-select", multiple = "multiple" })
            </div>

            <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
            <a asp-action="Index" class="btn btn-secondary">Hủy</a>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

